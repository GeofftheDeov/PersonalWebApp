node_modules/
backend/node_modules/
frontend/node_modules/
.env
certs/
.gemini/
changes.diff
/backend/dist/
.fuse_hidden*

# Phase 3 (#35): the merge sheet carries other people's names and emails and
# this repo is public; the map render and the patch files are both derived.
person-merge-adjudication.csv
wayfinder-map.html
patches/
