{
  "containerOverrides": [
    {
      "name": "mongodb",
      "command": ["sh", "-c", "echo 'Wiping corrupted WiredTiger data from dev EFS...' && find /data/db/dev-mongo5-data -type f -delete && find /data/db/dev-mongo5-data -mindepth 1 -type d -empty -delete && echo 'DONE - dev directory cleared'"]
    }
  ]
}
