{
  "containerOverrides": [
    {
      "name": "mongodb",
      "command": ["sh", "-c", "echo 'Wiping corrupted WiredTiger data from prod EFS...' && find /data/db/v7-data -type f -delete && find /data/db/v7-data -mindepth 1 -type d -empty -delete && echo 'DONE - prod directory cleared'"]
    }
  ]
}
